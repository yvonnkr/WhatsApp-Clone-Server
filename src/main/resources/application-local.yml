logging:
  level:
    ROOT: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    com.yvolabs.whatsappclone: DEBUG
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true